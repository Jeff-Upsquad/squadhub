{
  "name": "@squadhub/desktop-app",
  "version": "0.1.0",
  "private": true,
  "description": "SquadHub desktop — the SquadHub platform in a native window",
  "type": "module",
  "scripts": {
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
