{
  "name": "@squadhub/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "SquadHub mobile apps. Squad Chat native Android (Kotlin/Gradle) lives under ./android — see ./android/README.md. This package.json is only here so npm workspaces still resolve the directory."
}
