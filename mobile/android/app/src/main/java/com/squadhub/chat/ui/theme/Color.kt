package com.squadhub.chat.ui.theme

import androidx.compose.ui.graphics.Color

// --- Brand greens (reserved for FAB, outgoing bubble, active tab, accents) ---
val WaGreen = Color(0xFF00A884)           // modern WhatsApp accent green (2024+)
val WaGreenDeep = Color(0xFF008069)       // pressed / darker accent
val WaBubbleOutLight = Color(0xFFD9FDD3)  // outgoing bubble fill, light mode
val WaBubbleOutDark = Color(0xFF005C4B)   // outgoing bubble fill, dark mode

// --- Top bar / surface (post-2023 WhatsApp uses light surface, not green) ---
val WaTopBarLight = Color(0xFFFFFFFF)
val WaTopBarDark = Color(0xFF1F2C34)
val WaOnTopBarLight = Color(0xFF111B21)
val WaOnTopBarDark = Color(0xFFE9EDEF)

// --- Chat area backgrounds ---
val WaChatBgLight = Color(0xFFEFEAE2)     // classic cream
val WaChatBgDark = Color(0xFF0B141A)

// --- Incoming bubble ---
val WaBubbleInLight = Color(0xFFFFFFFF)
val WaBubbleInDark = Color(0xFF1F2C34)

// --- Standard surfaces ---
val WaSurfaceLight = Color(0xFFFFFFFF)
val WaSurfaceDark = Color(0xFF111B21)
val WaOnSurfaceLight = Color(0xFF111B21)
val WaOnSurfaceDark = Color(0xFFE9EDEF)

// --- Muted text (timestamps, previews, subtitles) ---
val WaMutedLight = Color(0xFF667781)
val WaMutedDark = Color(0xFF8696A0)

// --- Divider / outline ---
val WaOutlineLight = Color(0xFFE9EDEF)
val WaOutlineDark = Color(0xFF2A3942)

// --- Unread badge (green circle with white count) ---
val WaUnreadBadge = Color(0xFF25D366)

val WaError = Color(0xFFE03E3E)

// --- System-ish input field (composer background) ---
val WaComposerBgLight = Color(0xFFFFFFFF)
val WaComposerBgDark = Color(0xFF1F2C34)
val WaComposerBarLight = Color(0xFFF0F2F5)  // the strip around the composer
val WaComposerBarDark = Color(0xFF1F2C34)
